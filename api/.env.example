# Backend Environment Variables

# Server Configuration
PORT=4000
NODE_ENV=development

# Mailgun Email Configuration
# Get your API key from: https://app.mailgun.com/settings/api_security
# For sandbox domain, authorize recipients at: https://app.mailgun.com/app/sending/domains/[your-domain]
MAILGUN_API_KEY=your-mailgun-api-key-here
MAILGUN_DOMAIN=your-sandbox-domain.mailgun.org
EMAIL_FROM=postmaster@your-sandbox-domain.mailgun.org
EMAIL_TO=your-email@example.com

# CORS Configuration
# Comma-separated list of allowed origins
# Development: http://localhost:3000
# Production: https://moritz-waechter.de
ALLOWED_ORIGIN=http://localhost:3000

# Notes:
# 1. Sign up for Mailgun at https://www.mailgun.com/
# 2. For sandbox domain, you must authorize each recipient email in Mailgun dashboard
# 3. For production, add and verify your custom domain in Mailgun
# 4. Copy this file to .env and fill in your actual values
# 5. Never commit .env to version control
